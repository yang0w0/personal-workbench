# 贡献指南

感谢你帮助改进个人工作台。提交 Issue 或拉取请求前，请遵循以下规则。

## 开始之前

1. 先阅读 [README](README.md)、[安全政策](SECURITY.md) 和 [行为准则](CODE_OF_CONDUCT.md)。
2. 通过 Issue 说明较大的功能改动，避免重复工作。
3. 不提交 `data/`、备份、日志、密码、令牌、私人网址或任何个人资料。

## 开发要求

1. 前端继续保持为 `app/index.html` 中的自包含页面，不引入 CDN 或外部静态资源。
2. 本机 API 必须继续只监听 `127.0.0.1`。
3. 修改扫描、读取、写入或删除规则时，确认路径始终被限制在 `data/` 目录内。
4. 不要将未实现安全审计的功能描述为密码管理能力。
5. 新增用户可见行为、数据格式或运行要求时，同步更新相应文档。

## 提交前检查

```powershell
npm run check
git status
```

随后手动验证受影响流程：浏览器版先启动 `start-server.cmd` 再打开 `app/index.html`；桌面版则按 [客户端说明](docs/CLIENT.md) 构建和运行。使用拉取请求模板完成隐私与安全检查。
