# 贡献说明

1. 不提交 `data`、备份、日志、密码或任何个人网址列表。
2. 新功能优先保持前端为单个 `workbench.html`，不引入 CDN 或相对静态资源。
3. 本机 API 必须继续只监听 `127.0.0.1`。
4. 修改扫描或写入规则时，先确认路径始终被限制在 `data` 目录内。
5. 提交前检查 `git status`，并手动双击 `workbench.html` 验证页面可打开。
