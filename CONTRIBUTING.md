# 贡献指南

感谢你帮助改进个人工作台。提交 Issue 或拉取请求前，请遵循以下规则。

## 开始之前

1. 先阅读 [AGENTS.md](AGENTS.md)（模块边界规则）和 [接口总纲](docs/INTERFACES.md)，再读 [README](README.md)、[安全政策](SECURITY.md) 和 [行为准则](CODE_OF_CONDUCT.md)。
2. 通过 Issue 说明较大的功能改动，避免重复工作。
3. 不提交 `data/`、备份、日志、密码、令牌、私人网址或任何个人资料。

## 模块边界（本项目最重要的开发规则）

本项目按模块组织，**模块之间只通过接口文档通信**：

1. **只读、只改本次任务所属模块的文件。** 不要为了「了解上下文」通读整个仓库。
2. **需要用别的模块的能力时，查接口文档**（[docs/interfaces/](docs/interfaces/)），不要读它的实现代码。
3. **改接口 = 同一次改动里更新对应接口文档**，并在「变更记录」里留一行。
4. **必须跨模块改动时，先在 Issue/PR 里说明**「这需要同时改 Mx，理由是……」，不要静默改动。
5. 发现别的模块有 bug：写进 [INTERFACES.md 第 6 节](docs/INTERFACES.md#6-已知契约偏差必读)，**不要顺手修**。
6. 接口文档里不写真实数据，示例统一用 `example.com`。

模块与文件的对应关系见 [AGENTS.md](AGENTS.md) 的模块速查表；「我想改某个功能」的对照关系见 [任务路由表](docs/INTERFACES.md#4-任务路由表)。

## 开发要求

1. 前端继续保持为 `app/index.html` 中的自包含页面，不引入 CDN 或外部静态资源。
2. 本机 API 必须继续只监听 `127.0.0.1`。
3. `server/` 保持零第三方依赖。
4. 修改扫描、读取、写入或删除规则时，确认路径始终被限制在 `data/` 目录内。
5. 拖拽排序保持用 Pointer Events 实现，不要换成 HTML5 drag-and-drop（桌面端会失效）。
6. 不要将未实现安全审计的功能描述为密码管理能力。
7. 新增用户可见行为、数据格式或运行要求时，同步更新相应文档。

## 提交前检查

```powershell
npm run check
git status
```

`npm run check` 会同时校验服务端语法和接口文档结构。随后手动验证受影响流程：浏览器版先启动 `start-server.cmd` 再打开 `app/index.html`；桌面版则按 [客户端说明](docs/CLIENT.md) 构建和运行。使用拉取请求模板完成模块边界、隐私与安全检查。
