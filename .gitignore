# JavaScript dependencies
node_modules/

# Tauri/Rust build outputs
desktop/src-tauri/target/

# Locally built Windows packages (publish release assets separately)
*.exe
*.msi

# Local environment and editor files
.env
.env.*
!.env.example
.DS_Store
Thumbs.db
.vscode/

# Local workspace state kept by the assistant (notes, daily logs, caches)
.workbuddy/

# Personal workspace data: URLs, passwords, scripts, and local backups
data/
logs/

# Credentials, private keys, and local secret stores
.netrc
credentials*
*.pem
*.key
*.pfx
*.p12
*.kdbx
*.sqlite
*.db
