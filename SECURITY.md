# 安全政策

## 当前安全状态

该项目当前仅适合保存非敏感脚本和网址导航。`data/密码库` 只是预留目录，未实现加密密码库；请不要保存任何真实密码、恢复码、密钥或令牌。

## 本地服务边界

- 服务仅绑定 `127.0.0.1`，不要改为局域网或公网监听。
- 不要把 `data`、`logs` 或备份文件提交至 GitHub。
- 报告安全问题时，请不要在 issue 中附上真实敏感数据。

## 数据完整性

- `data/` 是唯一的真实数据副本。改动数据结构、扫描规则或删除逻辑之前，先备份 `data/`。
- 接口文档（`docs/interfaces/*.md`）只描述结构与规则，**不得写入真实网址、脚本内容、个人路径或密码**，示例统一用 `example.com`。
- 已知问题：桌面客户端执行写入操作会清掉 `data/catalog.json` 里的分类注册表（自建分类从界面消失，磁盘文件夹仍在）。修复前请优先使用浏览器版修改数据，见 [tauri-ipc.md](docs/interfaces/tauri-ipc.md#已知偏差)。

## 报告漏洞

请勿公开创建 Issue 报告安全漏洞。优先使用仓库的 GitHub 私密安全通告（Security Advisory）功能；若该功能尚未启用，请通过仓库维护者的 GitHub 账户私下联系，并仅提供复现所需的最少信息。

报告中请说明受影响版本、复现步骤、潜在影响和建议的修复方式。不要发送真实密码、令牌、密钥、私人网址、备份或完整个人数据文件。

## 密码库上线要求

密码功能必须在实现并审查以下内容后才可启用：客户端密钥派生、认证加密、随机 nonce、内存锁定清理、自动锁定、加密导出/导入、输入验证与独立安全测试。
